# tech-debt — CF2(cloudfuction-vps-ticket)债账

对齐 NEXT-Server tech-debt 惯例:只登值得单开 CP 的(低价值当场丢);清一条划 ✅ 留快照防丢根因。
触发条件不满足=defer 不动,满足=单开 CP 清。

---

## TD-1|console 按天观察:日桶趋势+Top IP 今日窗(2026-09-01 裁定 defer,用户「先不加了」)

**现状**:stats 纯内存三层(累计/今日桶/200 行日志环),无任何按天数据;Top IP=进程 lifetime
累计榜(停手滥用者霸榜、新冒头不可见);重启全清零。

**定案设计**(2026-09-01 与用户过完 ASCII,勿重新设计):
- 近 30 天日桶趋势条,落 hero 卡内(不加面);窗口=min(30, 存活天数)自适应,寿命<3 天整块隐藏
  (刚开/冷启动不出空图);断档=重启,如实留白不补零;悬停 title 带当日签发+限流数。
- 有 429/403 的日子条顶叠橙红帽(滥用从哪天开始一眼见)。
- Top IP 加「今日」列(现在谁在打我),排序改今日降序,累计降为次列——滥用使命优先。
- 数据走 ?json=1 增量通道(快照加 daily[] + per-IP today 字段,纯增量不动旧字段)。
- 跨重启=日桶落盘 stats.json(日期滚动+10min 定时写,~1KB);代价=打破零写盘部署契约,当时未裁。

**为何 defer**:内存版「近 30 天」承诺兑现不了(刚部署只有 1 天,发版即清零);落盘版引入
可写路径契约——两档都不干净,真实需求(按天看量/查滥用窗口)尚未落地,先不加。

**触发**:用户真要按天看量/排查某天滥用时解锁,单开 CP(设计已定案,直接进实现)。
